name: Build & Push Docker Images

on:
  push:
    tags:
      - 'ingress-v*'
      - 'node-v*'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    env:
      REGISTRY: ghcr.io
      OWNER: coolsamson7

    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      - uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine app & version
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          echo "Detected tag: $TAG"

          if [[ "$TAG" == ingress-* ]]; then
            APP=ingress
            VERSION=${TAG#ingress-v}
          elif [[ "$TAG" == node-* ]]; then
            APP=node
            VERSION=${TAG#node-v}
          else
            echo "No app matched for tag: $TAG"
            exit 1
          fi

          echo "APP=$APP"           >> $GITHUB_ENV
          echo "VERSION=$VERSION"   >> $GITHUB_ENV
          echo "Will build $APP version $VERSION"

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build & Push Docker Image
        run: |
          echo "Building ${{ env.APP }} version ${{ env.VERSION }}"
          ./gradlew --no-daemon :k8demo:${{ env.APP }}:jib \
            -Djib.to.image=${{ env.REGISTRY }}/${{ env.OWNER }}/${{ env.APP }}:${{ env.VERSION }} \
            -Djib.to.auth.username=${{ github.actor }} \
            -Djib.to.auth.password=${{ secrets.GITHUB_TOKEN }}

      - name: Update Helm values
        run: |
          VALUES_FILE="k8demo/helm/demo/values.yaml"

          sed -i "/^${{ env.APP }}:/,/tag:/ s/tag: .*/tag: \"${{ env.VERSION }}\"/" $VALUES_FILE

          echo "Updated $VALUES_FILE:"
          cat $VALUES_FILE

      - name: Commit & push updated Helm values
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add k8demo/helm/demo/values.yaml

          git diff --staged --quiet && echo "No changes to commit" && exit 0

          git commit -m "chore: bump ${{ env.APP }} image to ${{ env.VERSION }} [skip ci]"

          git fetch origin main
          git rebase origin/main

          git push origin HEAD:main